import { defineConfig } from '@playwright/test';
export default defineConfig({ testDir: './tests/browser', globalSetup: './tests/browser/setup.ts', globalTeardown: './tests/browser/teardown.ts', workers: 1, timeout: 60000, expect: { timeout: 15000 }, use: { baseURL: 'http://127.0.0.1:8081', headless: true, trace: 'retain-on-failure', screenshot: 'only-on-failure' }, reporter: [['list'], ['html', { open: 'never' }]] });
